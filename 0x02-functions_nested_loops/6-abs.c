#include "main.h"

/**
 *_abs - determines absolute value.
 *
 *@a:number to be computed.
 *
 * Return: the absolute value of the integer.
 *
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (-a);
}
